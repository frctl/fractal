{
  "name": "@frctl/twig-example",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build"
  },
  "dependencies": {
    "@frctl/fractal": "^1.5.6",
    "@frctl/mandelbrot": "^1.9.0",
    "@frctl/twig": "^1.2.8"
  },
  "devDependencies": {
    "@frctl/adapter-tests": "^0.2.0",
    "node-fetch": "^2.6.1"
  }
}
