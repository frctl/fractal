{
  "name": "@frctl/twig-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build"
  },
  "dependencies": {
    "@frctl/fractal": "^1.5.2",
    "@frctl/twig": "^1.2.4",
    "@frctl/mandelbrot": "^1.7.0"
  },
  "devDependencies": {
    "@frctl/adapter-tests": "^0.0.0",
    "node-fetch": "^2.6.1"
  }
}
