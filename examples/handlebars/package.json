{
  "name": "@frctl/handlebars-example",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build",
    "test": "jest",
    "test:dev": "npm run test -- --watch"
  },
  "dependencies": {
    "@frctl/fractal": "^1.5.1",
    "@frctl/handlebars": "^1.2.4",
    "@frctl/mandelbrot": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^26.0.1",
    "node-fetch": "^2.6.0"
  }
}
