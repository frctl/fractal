{
  "name": "@frctl/handlebars-example",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build"
  },
  "dependencies": {
    "@frctl/fractal": "^1.5.3",
    "@frctl/handlebars": "^1.2.6",
    "@frctl/mandelbrot": "^1.8.0"
  },
  "devDependencies": {
    "@frctl/adapter-tests": "^0.1.0",
    "node-fetch": "^2.6.1"
  }
}
