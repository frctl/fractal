'use strict';

const handlebars = require('handlebars');

module.exports = function(config){

    return {

        register(source, app){

        },

        render(file) {
            
        },

        instance: handlebars
    }

};
