<nav class="Navigation">
    {% if frctl.components.size %}
    <div class="Navigation-group">
    {{ nav.tree(frctl.components, entity, request) }}
    </div>
    {% endif %}
    {% if frctl.docs.size %}
    <div class="Navigation-group">
    {{ nav.tree(frctl.docs, page, request) }}
    </div>
    {% endif %}
</nav>
