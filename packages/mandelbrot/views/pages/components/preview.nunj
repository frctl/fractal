{% set entity = frctl.components.find('@' + frctl.request.params.handle) -%}
{% if not entity %}{{ throw(404, "Component '" + frctl.request.params.handle + "' not found.") }}{% endif -%}
{{ entity.render(null, true) | async }}
