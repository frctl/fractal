const {expect} = require('../../../../test/helpers');
const Router = require('./router');

describe('Router', function () {


});

describe('Router.buildRoutes()', function () {
  it('throws an error if an invalid builder is provided', function(){

  })
});
