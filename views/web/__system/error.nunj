{% extends '__system/skeleton.nunj' %}

{% set page = {
    type: 'error',
    title: frctl.web.request.error.name
} %}

{% block body %}
<div class="Error">
    
    <div class="Error-message">
    {{ frctl.web.request.error.message | markdown }}
    </div>

    {% if frctl.web.request.error.stack %}
    <code class="Error-stack">
        <pre>{{ frctl.web.request.error.stack }}</pre>
    </code>
    {% endif %}
</div>
{% endblock %}
