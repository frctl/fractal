<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        {# <link rel="stylesheet" href="{{ path('/styles.css') }}" media="screen" title="no title" charset="utf-8"> #}
        <title></title>
    </head>
    <body>

        <h1>{{ frctl.theme.loadPaths() }}</h1>

        {{ path('bar')}}

    </body>
</html>
